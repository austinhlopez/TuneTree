/* A sample require.js entry. We'll add more when we learn more. */
require(["lib/angular.min.js"], function(angular) {
    /* This function is called when lib/angular.min.js is loaded.
       if angular.js calls define(), then this function is 
       not fired until angular's dependencies have loaded, and the
       angular argument will hold the module value for "lib/angular.min.js*/
}