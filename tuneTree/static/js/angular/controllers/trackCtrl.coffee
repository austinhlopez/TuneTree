#Controller for rendering the primary track page.
window.tuneTree.app.ngApp.controller('trackCtrl', [
  "$scope"
  "$http"
  "$location"
  ($scope, $http, $location) ->
  # To do: Fill this stuff in right here.
])