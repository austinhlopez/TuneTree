#Controller for rendering each artist page.
window.tuneTree.app.ngApp.controller('artistCtrl', [
  "$scope"
  "$http"
  "$location"
  ($scope, $http, $location) ->
  # To do: Fill this stuff in right here.
])