#Controller for the track lyrics display page.
window.tuneTree.app.ngApp.controller('lyricsCtrl', [
  "$scope"
  "$http"
  "$location"
  ($scope, $http, $location) ->
  # To do: Fill this stuff in right here.
])