#Primary app controller.
window.tuneTree.app.ngApp.controller('ngAppCtrl', [
  "$scope"
  "$http"
  "$location"
  ($scope, $http, $location) ->

    #$scope.staticUrl = "/static/"
    # TODO. Use location service and RESTful URLs to organize
    # templateHead and templateBody locations.
    
])